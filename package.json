{
  "name": "cordova-plugin-android-debug-ssl-bypass-disable",
  "version": "1.0.0",
  "description": "A Cordova plugin to disable the default SSL error bypass in Android debug builds",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cordova",
    "ecosystem:cordova",
    "cordova-android"
  ],
  "author": "VoltBuilder",
  "license": "Apache-2.0",
  "type": "commonjs",
  "cordova": {
    "id": "cordova-plugin-android-debug-ssl-bypass-disable",
    "platforms": [
      "android"
    ]
  },
  "engines": {
    "cordovaDependencies": {
        "1.0.0": { "cordova-android": ">=4.0.0"}
    }
  }
}
